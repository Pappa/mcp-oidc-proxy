# Shared configuration for the auth-server and mcp-server workspace members.
# Copy to .env and adjust only if you change ports or client credentials.

DEMO_OIDC_CLIENT_ID=mcp-proxy-client
DEMO_OIDC_CLIENT_SECRET=dev-secret
AUTH_SERVER_URL=http://127.0.0.1:9000
MCP_SERVER_URL=http://127.0.0.1:8000
